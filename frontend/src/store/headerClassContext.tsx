import { createContext } from 'react'

export const headerWhiteContext = createContext('header')
export const headerPinkContext = createContext('headerPink')
export const headerClassContext = createContext('header')
